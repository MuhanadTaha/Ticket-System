@model TicketViewModel
@{
    ViewData["Title"] = "Edit";
}

<partial name="_CreateTicketsPartialView" model="@Model">

    @section scripts{
        @*عشان يصير الريكواير على مستوى الصفحة مش ع مستوى السيرفير يعني بمنع الريفريش*@
        <partial name="_ValidationScriptsPartial" />

        <script>

            //عشان يكون في ايديتور بالايريا
            tinymce.init(
                {
                    selector: 'textarea',
                    plugins: 'lists',
                    menubar: 'file edit format'
                }
            );
        </script>

    }
